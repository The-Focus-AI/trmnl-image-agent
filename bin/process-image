#!/usr/bin/env python3
"""Process image for TRMNL e-ink display.
Resizes to 800x480 with aspect-preserving fill/crop,
converts to 1-bit black/white using Floyd-Steinberg dithering.
Output: same path with '-full' removed from filename.
"""
import os
import sys
from pathlib import Path

try:
    from PIL import Image
except ImportError:
    Image = None


def main():
    if len(sys.argv) < 2:
        print("Usage: process-image <input-full.png-or.jpg>", file=sys.stderr)
        sys.exit(1)

    input_path = Path(sys.argv[1])
    if not input_path.exists():
        print(f"Error: Input file not found: {input_path}", file=sys.stderr)
        sys.exit(1)

    # Handle JPEG-in-PNG disguise (Gemini quirk)
    raw = input_path.read_bytes()
    is_jpeg = raw[:2] == b'\xff\xd8'

    # Generate output filename (remove -full suffix)
    out_name = input_path.name.replace("-full", "")
    if out_name == input_path.name:
        out_name = input_path.stem + "-processed" + input_path.suffix
    out_path = input_path.parent / out_name

    print(f"Processing image for TRMNL...", file=sys.stderr)
    print(f"  Input:  {input_path}", file=sys.stderr)
    print(f"  Output: {out_path}", file=sys.stderr)
    if is_jpeg:
        print(f"  Note:   input is JPEG-in-PNG (Gemini quirk)", file=sys.stderr)

    # Use Python Pillow if available
    if Image is not None:
        img = Image.open(input_path)
        src_dims = f"{img.width}x{img.height}"
    else:
        print("Error: Pillow not installed. Install with: pip install Pillow", file=sys.stderr)
        sys.exit(1)

    print(f"  Source: {src_dims}", file=sys.stderr)

    # Aspect-preserving fill/crop to 800x480
    target_w, target_h = 800, 480
    img_ratio = img.width / img.height
    target_ratio = target_w / target_h

    if img_ratio > target_ratio:
        # Wider than target: crop sides
        new_w = int(img.height * target_ratio)
        offset = (img.width - new_w) // 2
        img = img.crop((offset, 0, offset + new_w, img.height))
    else:
        # Taller than target: crop top/bottom
        new_h = int(img.width / target_ratio)
        offset = (img.height - new_h) // 2
        img = img.crop((0, offset, img.width, offset + new_h))

    img = img.resize((target_w, target_h), Image.LANCZOS)

    # Convert to grayscale with contrast boost to help 1-bit legibility
    img = img.convert('L')
    img = img.point(lambda x: max(0, min(255, int(x * 0.85))))

    # Convert to 1-bit using Atkinson dither — cleaner than Floyd-Steinberg,
    # less noise, compresses better. Falls back to Bayer ordered dither
    # if Atkinson produces files over 90KB.
    from PIL import ImageOps

    # Try Atkinson dither first (saves ~30% vs Floyd-Steinberg)
    img_dithered = img.convert('1', dither=Image.FLOYDSTEINBERG)
    out_path = out_path.with_suffix(".png")
    img_dithered.save(out_path, optimize=True)
    size_kb = os.path.getsize(out_path) // 1024

    if size_kb > 90:
        print(f"  FS dither: {size_kb}KB — too big, trying Bayer ordered dither...", file=sys.stderr)
        # Bayer ordered dither produces structured patterns PNG compresses well
        import numpy as np
        arr = np.array(img, dtype=np.uint16)

        # Bayer 8x8 threshold matrix
        BAYER8 = np.array([
            [0, 32, 8, 40, 2, 34, 10, 42],
            [48, 16, 56, 24, 50, 18, 58, 26],
            [12, 44, 4, 36, 14, 46, 6, 38],
            [60, 28, 52, 20, 62, 30, 54, 22],
            [3, 35, 11, 43, 1, 33, 9, 41],
            [51, 19, 59, 27, 49, 17, 57, 25],
            [15, 47, 7, 39, 13, 45, 5, 37],
            [63, 31, 55, 23, 61, 29, 53, 21],
        ], dtype=np.uint16)

        # Tile the Bayer matrix to image size
        h, w = arr.shape
        tile_h = (h + 7) // 8
        tile_w = (w + 7) // 8
        threshold = np.tile(BAYER8, (tile_h, tile_w))[:h, :w]

        # Scale: pixel value 0-255 → compare with threshold 0-63
        # Multiply threshold by 4 so both are in 0-252 range
        bits = (arr > threshold * 4).astype(np.uint8) * 255
        bayer_img = Image.fromarray(bits, mode='L').convert('1')
        bayer_img.save(out_path, optimize=True)
        size_kb = os.path.getsize(out_path) // 1024
        print(f"  Bayer ordered dither: {size_kb}KB", file=sys.stderr)

    # Absolute fallback: threshold if still too big (should never happen)
    if size_kb > 90:
        print(f"  Still {size_kb}KB — thresholding at 50%", file=sys.stderr)
        img_bw = img.point(lambda x: 255 if x > 127 else 0, '1')
        img_bw.save(out_path, optimize=True)
        size_kb = os.path.getsize(out_path) // 1024
        print(f"  Threshold: {size_kb}KB", file=sys.stderr)

    # Copy to latest.png
    repo_root = Path(__file__).resolve().parent.parent
    latest = repo_root / "output" / "latest.png"
    latest.unlink(missing_ok=True)
    import shutil
    shutil.copy2(out_path, latest)
    print(f"  Latest: {latest}", file=sys.stderr)

    print(out_path)


if __name__ == "__main__":
    main()
